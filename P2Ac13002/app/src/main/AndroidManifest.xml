<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.p2ac13002">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConsultaAnimalActivity"></activity>
        <activity android:name=".EliminarEspecieActivity" />
        <activity android:name=".InsertarPaisActivity" />
        <activity android:name=".InsertarAnimalActivity" />
        <activity android:name=".InsertarEspecieActivity" />
        <activity android:name=".MenuAnimalActivity" />
        <activity android:name=".MenuPaisActivity" />
        <activity android:name=".MenuEspecieActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>